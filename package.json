{
  "name": "space-pirates",
  "version": "0.1.0",
  "private": true,
  "description": "A minimal ambient space scene featuring one drifting spaceship.",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check src/main.js",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=18"
  }
}

